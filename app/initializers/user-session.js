export function initialize(container, application) {
   //application.inject('route', 'userSessionService', 'service:user-session');
   //application.inject('service:user-session', 'store', 'store:main');
}

export default {
  name: 'user-session',
  initialize: initialize
};
